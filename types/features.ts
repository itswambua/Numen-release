// types/Feature.ts (or a file of your choice)
export interface IFeature {
          icon: string;  // Assuming the icon is a string (e.g., 'book', 'users', etc.)
          title: string;
          description: string;
          iconClassName: string;  // Add the iconClassName field here
        }
        